@{
    AllNodes = @(
        @{
            NodeName = 'localhost'
            SMBShareConfig = "\\<server>\mof"
            SMBShareModules = "\\<server>\modules"
            CertificateFile = '...\*.cer'
            Thumbprint = '<>'
            RegistrationKey = "<>"
            }
        )
    }